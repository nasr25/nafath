# Used automatically by `npm run build`. Values are baked into the bundle
# (not secrets). New layout: Laravel is the IIS site root, this SPA is an
# application under it at /app.

# Backend is the same-origin site root, so API calls go to /api and /nafath.
VITE_API_BASE_URL=
